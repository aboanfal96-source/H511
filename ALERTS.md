# تنبيهات السوق — دليل التشغيل

تصلك التنبيهات **والمتصفّح مغلق، بلا أي مفتاح تضبطه**. كل شيء داخل مستودع H511 نفسه.

## كيف يعمل — بلا خادم ولا قاعدة بيانات ولا مفتاح

```
GitHub Actions (مجدول)
        │
        ├─ يجلب شموع 259 سهماً من مصدر البيانات
        ├─ يشغّل نفس محرّك المنصة حرفياً (engine/appvm.js يستخرج سكربت index.html)
        ├─ يقيّم الشروط (engine/alerts.js)
        ├─ يمنع تكرار ما سبق إرساله (.alerts-state.json)
        │
        ├─① يكتب التنبيهات في alerts-feed.json داخل المستودع
        │     ← المنصة تقرؤه عند فتحها: ترى ما صدر وأنت غائب، وإشعار متصفّح للجديد
        ├─② ينشر تعليقاً على مسألة «🔔 تنبيهات السوق — المنصة» في المستودع
        │     ← GitHub يُشعرك بالبريد وفي تطبيقه على الجوال
        └─③ تلقرام — اختياري فقط، إن أضفت مفتاحيه يوماً
```

القناتان ① و② تعملان بمفتاح Actions **المدمج** (`GITHUB_TOKEN`) الذي يمنحه
GitHub لكل تشغيل تلقائياً — ليس سرّاً تضيفه، ولا يخرج من المستودع.

## الضبط

**لا شيء مطلوب.** بعد أول تشغيل يجد تنبيهاً:
- تظهر مسألة جديدة في تبويب **Issues** اسمها «🔔 تنبيهات السوق — المنصة». كل
  تشغيل يضيف تعليقاً واحداً يذكرك (@) — فيصلك إشعار حتى لو لم تكن تراقب المستودع.
- لإشعار الجوال: ثبّت تطبيق **GitHub** وسجّل الدخول بحسابك، وفعّل إشعاراته.
- في المنصة: زر 🔔 ← قسم «📥 آخر ما وجده فاحص الخادم».

لإيقاف النشر في المسألة: `"githubIssue": false` في `alerts.config.json`.
ولإيقاف إشعارها وحدها: زر **Unsubscribe** في صفحة المسألة.

### تلقرام (اختياري)
إن أردته لاحقاً: `Settings → Secrets and variables → Actions` وأضف
`TELEGRAM_BOT_TOKEN` و `TELEGRAM_CHAT_ID`. غيابهما لا يُفشل شيئاً.

### جرّبها
تبويب `Actions` → «تنبيهات السوق» → `Run workflow`. **تجربة جافّة** تطبع ما كان
سيُنشر بلا نشر.

## المواعيد

كل **15 دقيقة** بين 10:07 و14:52 بتوقيت الرياض، الأحد–الخميس، وفحص أخير بعد الإغلاق (15:11).

> ⚠️ **قيس فعلياً:** بين 13 و24 سبتمبر نفّذ GitHub **19 تشغيلاً فقط** بدل نحو
> 21 في اليوم، وبعضها بعد الإغلاق بساعات. GitHub يُسقط التشغيلات المجدولة أو
> يؤخّرها عند الازدحام، وأشدّه عند رأس الساعة وأرباعها — وهي الدقائق التي كانت
> مستعملة. نُقلت الجدولة إلى دقائق فردية (7، 22، 37، 52) لتقليل ذلك، لكن لا
> ضمان. وأي تنبيه يصدر والسوق مغلق يحمل سطراً يقول إن التنفيذ للجلسة القادمة.

> ⚠️ جدولة GitHub تقريبية بطبعها: التشغيل يتأخّر دقائق وقد يبلغ التأخير عشرين
> دقيقة في أوقات الذروة، وأقصر فاصل مسموح خمس دقائق. هذه أداة «لا تفوّت
> الفرصة» لا أداة تنفيذ لحظي. الدقّة إلى الثانية تحتاج خادماً دائماً، وهذا ليس مجانياً.

> ⚠️ GitHub يوقف المهامّ المجدولة تلقائياً بعد 60 يوماً بلا نشاط في المستودع.
> أعِد تفعيلها من تبويب Actions إن حدث.

## الإعدادات — `alerts.config.json`

| المفتاح | المعنى |
|---|---|
| `kinds` | `ready` الشروط مكتملة · `zone` السعر دخل منطقة الدخول · `approach` يقترب · `radar` دورة زمنية قريبة |
| `cooldownHours` | لا يُعاد التنبيه نفسه قبل هذه المدة إلا إذا تغيّرت الخطة (منطقة أو وقف جديد) |
| `maxPerBatch` | سقف الرسائل في التشغيل الواحد؛ الباقي ينتظر التشغيل التالي. الأولوية للأقرب وقفاً بوحدات ATR ثم الأعلى عائداً — لا لأصغر رمز كما كان (فكانت البنوك تأخذ المقاعد الثمانية في كل تشغيل) |
| `symbols` | `null` = كل السوق، أو مصفوفة رموز لمتابعة مختارة |

`radar` **بطيء** (تحليل طيفي لكل سهم، ~40 ثانية إضافية) و**مطفأ افتراضياً**.
القياس على السوق الحقيقي (24 سبتمبر): 15 من 257 سهماً تحت p = 0.05، أي بمعدّل
الصدفة تماماً. الخريطة تُصحَّح الآن بـ Benjamini-Hochberg عبر السوق كله قبل أي
تنبيه، فلا يبقى إلا ما يتجاوز الصدفة — وعلى السوق الحالي لا شيء يتجاوزها.

> 🛠️ وكان الرادار على الخادم معطّلاً بصمت قبل هذا الإصلاح: الخريطة كانت تُكتب في
> متغيّر لا يُرى من خارج الصفحة، فيرمي كل سهم خطأً يُبتلع. لو فُعِّل `radar` لما
> صدر منه شيء أبداً.

## ما لا يُنبَّه عليه عمداً

1. **وقف داخل ضجيج الجلسة** (أضيق من نصف ATR) — يُنتج نسبة عائد/مخاطرة
   مضخّمة فتبدو صفقة رديئة هي الأفضل.
2. **منطقة دخول مشتقّة من ATR** — هذا النطاق يُبنى حول السعر الحالي بالتعريف
   فالسعر داخله دائماً. القياس على 254 سهماً حيّاً: 110 منها كانت ستُطلق
   تنبيهاً بهذا السبب وحده. التنبيه يكون على بنية مرصودة: فجوة قيمة عادلة
   أو كتلة أوامر أو نطاق تأكيد اختراق.
3. **هدف بعائد/مخاطرة دون 1:1.5** بعد وضع الوقف تحت منطقة الدخول.

## التشغيل محليّاً

```bash
node scripts/alert-scan.js --dry              # يطبع ما كان سيُرسل، بلا إرسال
node scripts/alert-scan.js --dry --limit 20   # عيّنة سريعة
npm run scanner                               # اختبارات الفاحص
```

متغيّرات بيئة اختيارية: `STOCK_API_BASE` (وسيط بدل المصدر المباشر)،
`SITE_URL` (الرابط في ذيل الرسالة).

## ملف الحالة

`.alerts-state.json` يُحفظ في المستودع بعد كل تشغيل أرسل شيئاً، برسالة
`[skip ci]` فلا يُطلق نشراً. يحمل مفاتيح منع التكرار وتشخيص آخر تشغيل
(كم فُحص، كم مرشّح، كم أُرسل، كم فشل جلبه). امسحه لتُعاد كل التنبيهات.

## الفرق بين هذا وتنبيهات المتصفّح

| | داخل المنصة | GitHub Actions |
|---|---|---|
| يعمل والمتصفّح مغلق | ✗ | ✓ |
| فوري عند تحديث الصفحة | ✓ | كل 15 دقيقة (± تأخير) |
| يحتاج ضبطاً | لا | مفتاحان مرّة واحدة |
| نطاق الفحص | الأسهم المحمّلة | كل السوق |

الاثنان يعملان معاً بلا تعارض: لكلٍّ ملف حالة منفصل.
